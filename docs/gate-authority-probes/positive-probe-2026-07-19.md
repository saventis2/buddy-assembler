# Gate Authority Positive Probe

This inert documentation-only change verifies that the base-owned Gate Authority
sentinel accepts a pull request whose workflow and protected authority files are
byte-for-byte identical to the trusted `main` base.

The probe pull request is evidence only. It must not be merged.
